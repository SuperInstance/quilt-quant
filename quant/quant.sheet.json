{
 "id": "quilt-quant-desk",
 "title": "The Trading Desk — backtest as a spreadsheet",
 "cells": [
  {
   "id": "bt.last",
   "kind": "value",
   "value": null,
   "description": "last full desk receipt (published by bt.run)"
  },
  {
   "id": "bnh.last",
   "kind": "value",
   "value": null,
   "description": "last buy & hold receipt"
  },
  {
   "id": "wf.last",
   "kind": "value",
   "value": null,
   "description": "last walk-forward report (published by wf.report)"
  },
  {
   "id": "mkt.meta",
   "kind": "value",
   "value": {
    "n": 760,
    "start": 100,
    "seed": 20260925,
    "source": "synthetic-gbm-regimes",
    "regimes": [
     "trend-up×120",
     "bear×90",
     "trend-up×110",
     "chop×80",
     "trend-up×120",
     "bear×100",
     "recovery×140"
    ]
   },
   "description": "the instrument tape: synthetic GBM with regimes (seeded, reproducible)"
  },
  {
   "id": "mkt.closes",
   "kind": "value",
   "value": [
    99.44292331072151,
    100.20477140801503,
    100.64297875142817,
    100.58772627220009,
    100.63083143033974,
    101.73300516109954,
    101.26414784395797,
    100.96869043471148,
    101.24081769891158,
    99.59643237657967,
    100.8659226710284,
    101.0948935266639,
    102.47835108759485,
    102.857038574708,
    102.43216282626457,
    100.76893176418594,
    101.00266779067785,
    102.18117569256364,
    101.9054855711367,
    103.00408659290811,
    104.46695518988915,
    103.60345321085202,
    104.58740965590687,
    104.68036717716049,
    105.54182601032964,
    105.69093936436454,
    106.84295212062236,
    105.90568672474585,
    106.24681326434991,
    106.46239652311183,
    107.27851593064223,
    106.30275077246367,
    106.28411139127236,
    106.09746717476534,
    106.7729175616,
    107.52853286439668,
    106.70374020439785,
    105.8123795472808,
    107.2126637494917,
    106.45458426180905,
    105.47753443805071,
    105.70711949084038,
    106.98715241736235,
    107.85649601554924,
    107.78839839473352,
    107.09716861524993,
    107.18129814765709,
    108.13475004857928,
    108.7447524872468,
    109.06691534719577,
    108.67960662793647,
    111.12903631869335,
    111.84648553882596,
    113.00880556931487,
    114.17255289510649,
    114.35120712738654,
    113.73543039867559,
    114.36265132408421,
    114.58561073495773,
    114.63322014930395,
    114.83135160471471,
    113.7360436713837,
    114.3617975800708,
    115.35453122955576,
    116.37836246854894,
    116.63313929048779,
    117.47339182589846,
    117.96434858553425,
    120.71341305435462,
    119.74357087692182,
    118.53132707777932,
    121.16821660075738,
    120.15583211098898,
    119.25547214106135,
    119.90525017295732,
    119.96803941792268,
    120.4200903307386,
    119.33480246373104,
    119.21338887263968,
    118.86462725355835,
    119.20404526019995,
    121.93536017876312,
    123.05930572777183,
    124.59593777237697,
    124.77229021894355,
    125.0390061737932,
    123.2379595018017,
    123.04423355668072,
    124.23223118936143,
    123.74300292156829,
    123.2376540987191,
    126.09034777236586,
    126.42336443256553,
    125.13755822375437,
    125.2626682900594,
    125.4925128282679,
    125.59740848879547,
    125.28439253805354,
    126.43032273032988,
    126.42410744684507,
    127.9215061343926,
    128.7686479198976,
    129.28044218423,
    129.95682468679766,
    130.939177420437,
    130.00655295090286,
    130.09355506874226,
    130.01327036633623,
    130.0112044284378,
    128.99740979299526,
    129.73992519020513,
    129.09348403117275,
    129.03032155538253,
    127.58211451598694,
    126.87839447718181,
    128.2246390284056,
    127.13247656191625,
    127.25182425527008,
    127.01717444243772,
    125.52852292664856,
    123.41642738530402,
    124.72811823063422,
    125.76112595170552,
    125.94031943019415,
    127.61834118144179,
    128.70386776653163,
    129.2988323567169,
    125.99882302908604,
    128.29034554096188,
    129.947572795474,
    130.24931055462588,
    130.15760103839534,
    128.87502395738994,
    127.06962740274413,
    126.25749325986992,
    124.83448720643085,
    121.2014786401099,
    126.12876828757362,
    125.5801974548968,
    123.74503159550787,
    123.06942122156862,
    121.57029219059918,
    121.95085191138519,
    123.17643013587244,
    123.37220048003981,
    122.05333251209845,
    121.62298957613304,
    124.72683389930292,
    124.68336458138661,
    122.76697375661954,
    120.903523349712,
    119.3847974983533,
    121.49772244949303,
    122.29362797417575,
    121.69703109191357,
    125.15014690388581,
    125.36721295488587,
    126.23933410012148,
    126.31157590028042,
    124.98299010535212,
    125.88927129690542,
    122.56508678518264,
    123.10667931003947,
    123.95293978297204,
    122.98784818474769,
    125.49721240363907,
    124.74922464363458,
    124.47465002581146,
    125.43946866475342,
    128.16795729673194,
    129.62055919248922,
    131.38413601510538,
    136.40664354256347,
    136.27439265384055,
    135.93774023773875,
    135.58483829663578,
    136.0560351259265,
    136.1956100029573,
    134.82244418417045,
    131.53258812836043,
    129.63345496348958,
    125.8098101863362,
    126.95695170881139,
    126.32177837700323,
    126.79760327115122,
    128.43586277540788,
    128.09245372039197,
    125.80046555308519,
    126.48834472966294,
    125.51909057561262,
    128.2955245297869,
    126.68541235735253,
    128.3022875694659,
    128.9852917662678,
    124.00315380673337,
    125.19691818217494,
    125.85594803076978,
    128.13915428789377,
    128.95762405692636,
    130.5680009890843,
    128.04529189028793,
    126.65094018638058,
    128.61679716523307,
    127.92793411370553,
    127.2078341956478,
    127.2128756712761,
    126.89054961549981,
    129.35327257977926,
    129.9225177292216,
    126.99512011695644,
    124.76666858634403,
    123.63850059546782,
    123.70884273574647,
    126.75719001888703,
    127.60326356657872,
    129.54755435530663,
    130.12640580459598,
    131.88472569963974,
    133.9920373810886,
    132.86081807994398,
    132.3041941866395,
    131.88560232532544,
    131.57358046744935,
    130.97905616891367,
    129.34808345717985,
    130.335689269633,
    131.56900379195125,
    132.3158041150343,
    132.08950164518697,
    130.3116652665499,
    131.32805255220205,
    130.9690571895793,
    133.19528417295027,
    133.5626251624287,
    132.59490014843863,
    130.82718761824145,
    129.11903178496402,
    129.92439089408344,
    132.4518981714883,
    133.26187741640896,
    136.18214494725518,
    136.74300328147262,
    136.8261098448404,
    135.9250557967524,
    135.32268272013218,
    134.53150596714056,
    132.38060275137468,
    131.9622437737337,
    133.60205572112773,
    135.81122313260727,
    136.0662514737951,
    134.16061267268427,
    133.22811798162616,
    136.0787359184468,
    136.88144032303236,
    136.06036915461127,
    137.61951669158407,
    137.9514290174305,
    138.17231107855395,
    141.25128581389788,
    142.27711079629046,
    141.87552694158268,
    143.32673463684682,
    144.0013686631203,
    144.32784210774628,
    146.4839734144544,
    148.25075441202173,
    150.38354013653895,
    146.9198258920782,
    146.64623811025513,
    147.7764115242144,
    148.5965842851848,
    147.625270198394,
    148.28703707340657,
    146.7378838749688,
    148.54037054867786,
    151.03290824358774,
    153.68078069557365,
    152.39283392331927,
    150.8890914735476,
    150.41545066375494,
    152.81472476464052,
    154.4614553679429,
    154.8253757307135,
    156.5034090460828,
    154.4685555043212,
    153.45980013017913,
    153.04811916928665,
    152.71059638676303,
    154.0854722385423,
    153.58798737100355,
    153.3332140050082,
    152.43423481340653,
    153.063192683813,
    154.2626893889505,
    155.85859904666844,
    155.94038290247695,
    156.02622343736604,
    156.42733427157043,
    157.70277315062668,
    159.1125207792697,
    161.69148862852347,
    161.4120660011039,
    162.07374868971073,
    162.70544642325197,
    162.21299579592295,
    163.62504215722498,
    160.91204284695473,
    160.0917808892019,
    160.8448734509055,
    158.4440244241334,
    156.12667856587495,
    156.27891061218244,
    157.23477376585544,
    156.7916841407334,
    157.49464429405631,
    156.46469690968314,
    155.76108934943866,
    155.86797724204771,
    155.66837666447714,
    151.75730665965455,
    153.27449128723288,
    151.3860818374081,
    150.99385406426347,
    152.17582481103065,
    152.312224650946,
    153.37104720156103,
    150.4739273583068,
    149.7565626946563,
    150.3486541665117,
    146.32147729518263,
    148.73322578439314,
    151.3173358537509,
    155.45331961626945,
    158.7568120813374,
    158.4535908388075,
    161.0507723533047,
    164.1544217914009,
    161.50780712347256,
    160.54142171978177,
    161.8536640514241,
    162.70599582523766,
    159.14112896103615,
    167.49473057488717,
    164.61865224118745,
    160.65656278829402,
    164.33226702509674,
    165.71941593114875,
    166.4763818025196,
    164.0249571770087,
    160.97253393065645,
    163.18719605769232,
    158.42356648241005,
    162.93388543022647,
    158.69806591299263,
    162.55348720624866,
    163.5696648159863,
    163.40861862302643,
    165.067644893643,
    164.38724353337125,
    164.83093334900815,
    165.11494003212144,
    166.68495226273572,
    169.19900995324812,
    168.16408540075966,
    169.9239226010949,
    170.1515796195269,
    165.3127106352746,
    167.2436049301389,
    166.61703181287547,
    165.2232486265727,
    161.7602915091223,
    163.69056846892215,
    165.9791224807469,
    170.09432935215153,
    169.39508645566502,
    169.2821382339961,
    168.58597362921373,
    164.48102011994263,
    165.13709090604794,
    164.21829506388465,
    165.78685941993285,
    165.97211959180112,
    165.05149342789764,
    163.76137261397034,
    159.91083240801487,
    157.3294638136592,
    161.94956974530888,
    159.04334067785385,
    160.85058253309484,
    166.4198988773793,
    168.64894259439734,
    169.1799814452091,
    167.86666389012535,
    166.97516322437096,
    166.619561755331,
    169.3538747086016,
    171.28622082170924,
    177.81515953069976,
    174.40563680183655,
    175.7565263315726,
    174.6536313736978,
    173.7826578074866,
    173.05663974102737,
    173.07977330792798,
    173.91199315131908,
    176.34392619728524,
    176.15363505453243,
    177.22463692238588,
    179.29330688514946,
    177.69560937253536,
    178.32539690436522,
    178.92865883941937,
    181.26717609089488,
    180.52937408749276,
    180.7889118891121,
    179.96260494030813,
    182.1929080626903,
    182.2113654237687,
    182.97701707851093,
    182.87598608553535,
    181.46380700601654,
    179.12277184636628,
    178.74632359217068,
    178.17330081847507,
    177.34767795626755,
    178.09630201694145,
    177.87519144921094,
    174.94446487427825,
    175.3113194707259,
    173.6898860125706,
    173.01531300469142,
    173.16568677570353,
    171.69534404820095,
    172.35411381510076,
    170.81672221114775,
    171.7764603079037,
    173.1925194653307,
    172.39055392736793,
    170.1674251414433,
    171.95304851229616,
    171.73647475061443,
    174.15633517357125,
    172.45393441611034,
    171.44260248868537,
    172.5976877038338,
    171.94649467295326,
    172.29696941416276,
    171.09201490204615,
    173.5421566752825,
    173.6756222708709,
    176.45761824036066,
    175.9116480553139,
    174.03198329737262,
    174.1738549966902,
    170.14250826323953,
    168.97436311044098,
    171.93518807666672,
    170.6503958385862,
    170.8802699559633,
    170.6772380058258,
    168.9229310091379,
    170.57827580546163,
    171.75664822234342,
    172.2271993482522,
    172.22382464602137,
    169.87074841481075,
    172.1610441440139,
    173.01263907356838,
    175.20463636204167,
    174.25784293298966,
    176.64637236166112,
    178.32093424428143,
    180.32965509764176,
    184.14323000480468,
    183.2847185048999,
    182.0667693321559,
    180.1925277107592,
    179.31010349281607,
    179.47552196261634,
    179.59735271364517,
    179.2663790946631,
    180.2904190675469,
    182.02244594321138,
    182.07403404538593,
    180.45334847839214,
    180.21134254512071,
    180.65773859249455,
    183.10472140552238,
    186.0130874446802,
    187.95577765210567,
    188.8668387878662,
    188.1064565480435,
    185.0759097762304,
    186.38706666727526,
    186.76840344856956,
    184.08630590151162,
    185.32586489905805,
    183.73805477664428,
    182.7628208132824,
    185.00828122240242,
    182.77720498331746,
    183.87432907945188,
    182.0021342537102,
    182.61347342064545,
    181.42794599010904,
    181.26036404127015,
    184.11673886606243,
    180.12864864760388,
    181.13983534508176,
    180.44952396903483,
    184.76228332124285,
    183.00152508666582,
    182.03994645823198,
    180.26597936531732,
    182.2671544337565,
    183.6612316938423,
    185.52498045432156,
    185.13765807745605,
    185.6353002481132,
    179.2541929079326,
    178.58199259236855,
    179.66808712275179,
    177.64411978066093,
    175.28100577942007,
    174.1318499005427,
    169.89450979392728,
    168.97660305741337,
    168.7358571411206,
    173.36386731672275,
    170.60713656382302,
    171.22297919861433,
    174.2507242693058,
    173.30362752151396,
    176.46529852045524,
    176.5992944829121,
    177.30563466576405,
    178.91270088301107,
    175.08490991977905,
    175.3113306546729,
    173.9540143124652,
    174.80476698181175,
    174.74583068770534,
    173.2722199752871,
    172.0473466631137,
    171.77472053360216,
    172.9190381831346,
    174.99904379132346,
    176.64851598144628,
    174.83062000836867,
    174.83973072927108,
    172.37937964330604,
    170.9832630786222,
    171.61700832203147,
    170.6053995454369,
    171.4290702272994,
    170.72972649233938,
    172.11577463993785,
    172.2510824167531,
    167.85111700103383,
    166.7927734782734,
    163.83581288807127,
    165.24417834402126,
    163.93926279003827,
    163.42206006974197,
    161.94102240706258,
    156.53761011974956,
    157.74786199371925,
    156.66574156291523,
    157.32205203619893,
    159.12150511016935,
    159.77181644525018,
    161.1437354197175,
    162.39690327150498,
    162.53322408656263,
    162.28691818190566,
    166.9687542047992,
    169.96107127417645,
    169.64010355703556,
    166.8181173925975,
    168.3334969880758,
    168.90697756204185,
    165.602725754398,
    165.24155024901106,
    165.65385180863703,
    167.6470975799897,
    169.4566978337924,
    170.1128639877152,
    172.3572730657564,
    175.20386922155384,
    173.28553307136764,
    175.1874835442904,
    170.4190252754113,
    168.74003002689207,
    167.27262138704515,
    170.7331101671267,
    169.3786064546972,
    166.3553532962276,
    173.65525045088867,
    173.51485547301766,
    171.4879379590968,
    172.7565122840223,
    170.32875210018986,
    168.31161571280262,
    171.40431695141984,
    173.67291174766476,
    173.03241056624879,
    174.25710576978395,
    173.7949119328294,
    173.19570307323676,
    175.32129972276076,
    173.2422768001428,
    172.91077910598045,
    171.17058666898015,
    174.55051105751477,
    176.73178880561008,
    179.73038747300023,
    177.49312572251586,
    177.51912067850537,
    175.01150361343346,
    176.00438578736006,
    177.90917173420223,
    177.28057304722896,
    174.55952342794785,
    174.87080575875936,
    175.09471508647366,
    173.11387862296098,
    171.59018130559033,
    172.62662133973598,
    169.86021504525633,
    167.79070344764753,
    168.3774840591894,
    167.0838259717102,
    166.38854997292108,
    166.0065676430122,
    165.8087616035718,
    166.77277761768588,
    167.82150738756906,
    165.7598681343762,
    165.9634188779535,
    167.06219095493532,
    167.68741690979866,
    167.7671631442692,
    165.96649228048312,
    167.85371055409735,
    169.56260423694314,
    169.36575609337117,
    172.80479786261878,
    172.7781517847769,
    171.88127282145757,
    171.5039044604733,
    170.86569423556242,
    169.34707370272744,
    168.0845333568322,
    167.7421714480806,
    167.61651289377812,
    166.92791484375098,
    167.69713668285132,
    167.5260129028087,
    166.17012067972257,
    167.12408971403627,
    167.86180098859003,
    166.58599673369181,
    165.73802859612448,
    166.8662039464801,
    166.99368667582448,
    166.50531740455835,
    170.55731077570712,
    170.60320193650472,
    172.29856817901265,
    171.67761598285594,
    174.86479636276727,
    173.54722880751189,
    173.90650826763905,
    176.2857141239316,
    176.9522333799757,
    175.53712444994824,
    178.86939979304879,
    175.72359130266972,
    174.9064872877516,
    173.36625033084908,
    172.90603238825133,
    171.22025933154714,
    171.35484565994477,
    176.80144787307407,
    178.4470030128067,
    179.66121864147323,
    177.29242090866921,
    177.93748688822387,
    178.34400147980344,
    179.56554086030312,
    176.10724863946484,
    173.25801840141375,
    174.09974240435713,
    175.42301409785932,
    174.08472744443867,
    173.97812100784074,
    175.2272633368908,
    176.4111731739627,
    176.4629842883149,
    177.7279361457788,
    178.36273293343174,
    174.3587912149266,
    172.85133730121115,
    171.93268182753036,
    172.1655133844784,
    170.81977099946965,
    171.17854221778035,
    173.8281527683299,
    177.65190488024007,
    180.60516159084443,
    180.71345062393937,
    178.84381601092718,
    180.52184573852708,
    180.098944480209,
    182.338872811155,
    182.82003142392574,
    182.61507029950357,
    180.27037801838128,
    179.7774724570264,
    181.90429409611434,
    182.87316009140577,
    182.2032623191794,
    182.62302433259708,
    186.55617322842878,
    188.66047970348225,
    188.8797250618682,
    189.99805016238125,
    191.4100415948639,
    191.80131642444525,
    195.6081408563749,
    197.4565929430817,
    196.2426839361956,
    200.63107154910577,
    203.1381461719321,
    201.20444581010668,
    202.0874695317701,
    203.4097743976952,
    201.23031242870962,
    201.3975672002009,
    199.99682786955864,
    202.32214486405707,
    203.03803668428668,
    203.6833558871135,
    203.57587562526646,
    204.06248308974028,
    205.29443493750128,
    206.9828452084268,
    209.14967113004852,
    207.7131869584826,
    204.4710416054889,
    205.05942600700052,
    206.90903557956833,
    207.8409945144307,
    206.0698085013666,
    204.37676267399894,
    205.51321680961019,
    205.77682021438073,
    209.07115825348959,
    209.3130524559424
   ],
   "description": "daily closes — the only market input the desk may see"
  },
  {
   "id": "desk.cash0",
   "kind": "value",
   "value": 10000,
   "description": "starting cash (S1)"
  },
  {
   "id": "desk.flash",
   "kind": "value",
   "value": {
    "kind": "",
    "text": "desk ready — nudge a parameter cell or run the trainer.",
    "ts": 0
   },
   "description": "the desk bubble (refusals, promotions)"
  },
  {
   "id": "desk.prev_champ",
   "kind": "value",
   "value": null,
   "description": "previous champion (for the promotion flash)"
  },
  {
   "id": "desk.champion",
   "kind": "value",
   "value": {
    "gen": 0,
    "strategy": "sma_cross",
    "params": {
     "fast": 8,
     "slow": 34,
     "rsi_len": 14,
     "rsi_max": 72,
     "rsi_buy": 30,
     "rsi_sell": 64
    },
    "is_score": null,
    "oos_score": null,
    "verdict": "—"
   },
   "description": "the reigning config (S7)"
  },
  {
   "id": "log.events",
   "kind": "value",
   "value": [],
   "description": "desk event log"
  },
  {
   "id": "p.strategy",
   "kind": "value",
   "value": "sma_cross",
   "description": "active strategy: sma_cross | rsi_reversion (S3)"
  },
  {
   "id": "p.fast",
   "kind": "value",
   "value": 8,
   "description": "sma_fast window (S2)"
  },
  {
   "id": "p.slow",
   "kind": "value",
   "value": 34,
   "description": "sma_slow window (S2)"
  },
  {
   "id": "p.rsi_len",
   "kind": "value",
   "value": 14,
   "description": "RSI period (S2)"
  },
  {
   "id": "p.rsi_max",
   "kind": "value",
   "value": 72,
   "description": "overbought gate for new sma_cross entries (S3)"
  },
  {
   "id": "p.rsi_buy",
   "kind": "value",
   "value": 30,
   "description": "oversold entry for rsi_reversion (S3)"
  },
  {
   "id": "p.rsi_sell",
   "kind": "value",
   "value": 64,
   "description": "overbought exit for rsi_reversion (S3)"
  },
  {
   "id": "p.fee_bps",
   "kind": "value",
   "value": 2,
   "description": "cost per position change, basis points of notional (S4)"
  },
  {
   "id": "p.wf_pct",
   "kind": "value",
   "value": 70,
   "description": "share of bars in the FIT window; the rest is the JUDGE window (S6)"
  },
  {
   "id": "strategy.book",
   "kind": "value",
   "value": "QUANT DESK — STRATEGY BOOK v1 (one instrument, daily closes)\n\nS1 (universe)     The desk prices ONE instrument from a single series of daily\n                  closes (mkt.closes). Cash starts at desk.cash0. A series\n                  that is shorter than 50 bars, or contains a bar that is not\n                  a finite positive number, cannot be traded: S1 stops the\n                  desk and nothing downstream may run.\n\nS2 (indicators)   sma_w is the arithmetic mean of the last w closes; it does\n                  not exist until w closes have printed. rsi_n is Wilder's\n                  RSI over closes with period n; it does not exist until n+1\n                  closes have printed. An indicator cell that does not exist\n                  yields FLAT, never a guess.\n\nS3 (signal)       Two strategies may run, one at a time (p.strategy).\n                  sma_cross: LONG (+1) when sma_fast > sma_slow AND\n                  rsi <= p.rsi_max (the overbought gate); FLAT otherwise.\n                  rsi_reversion: LONG when rsi <= p.rsi_buy; FLAT when\n                  rsi >= p.rsi_sell; while in between, HOLD the previous\n                  position. A signal may only use bars up to and including\n                  the bar it is computed on (no lookahead), and it acts at\n                  the NEXT close: position[i] earns close[i+1] - close[i].\n\nS4 (costs)        Every position CHANGE pays p.fee_bps of the traded\n                  notional (price * |change|) at the close where the trade\n                  happens. No leverage: the position is 0 or 1 unit. A trade\n                  that is still open at the last bar is closed there and\n                  pays its exit fee like any other.\n\nS5 (metrics)      The desk reports: total_return, CAGR (252d year), annual-\n                  ized Sharpe (rf=0), max_drawdown, n_trades, win_rate\n                  (per round trip incl. fees), profit_factor, exposure.\n                  A metric that is not a finite number of the right shape\n                  stops the desk (S5 refuses to print nonsense).\n\nS6 (walk-forward) The first p.wf_pct of bars is the FIT window (IS); the\n                  rest is the JUDGE window (OOS). IS is for fitting, OOS is\n                  for judging. A config is ROBUST when OOS sharpe >= 0.75,\n                  OOS max_drawdown <= 40% and OOS n_trades >= 3; a config\n                  that wins in-sample while losing out-of-sample is\n                  OVERFIT; everything else is WEAK. Both windows are always\n                  reported; printing only the flattering one is forbidden.\n\nS7 (promotion)    A candidate replaces the desk champion only if BOTH hold:\n                  its OOS score beats the champion's OOS score, AND its IS\n                  score does not degrade the champion's IS score. Score =\n                  sharpe - 2*maxdd - (0.5 if fewer than 3 trades). Every\n                  promotion and every refusal is booked as a witness\n                  receipt in the ai.ledger chain.\n\nS8 (audit)        The desk is recomputable: every number derives from\n                  mkt.closes + parameter cells + this book. The ai.ledger\n                  is an fnv1a64 hash chain rooted at GENESIS; S8 re-derives\n                  it and any tampered row breaks the chain visibly. The\n                  trainer may nudge parameter cells only; the book itself\n                  may not be edited mid-run.",
   "description": "the complete trading doctrine S1..S8"
  },
  {
   "id": "rule.S1.law",
   "kind": "value",
   "value": "The desk prices ONE instrument from mkt.closes; a series shorter than 50 bars or containing a non-finite/non-positive bar stops the desk.",
   "description": "clause S1"
  },
  {
   "id": "rule.S2.law",
   "kind": "value",
   "value": "sma_w needs w closes; rsi_n needs n+1 closes; a missing indicator yields FLAT, never a guess. Parameters must be in tradeable ranges.",
   "description": "clause S2"
  },
  {
   "id": "rule.S3.law",
   "kind": "value",
   "value": "sma_cross: LONG when sma_fast > sma_slow AND rsi <= rsi_max. rsi_reversion: LONG when rsi <= rsi_buy, FLAT when rsi >= rsi_sell, HOLD between. No lookahead; act at the next close.",
   "description": "clause S3"
  },
  {
   "id": "rule.S4.law",
   "kind": "value",
   "value": "Every position change pays fee_bps of traded notional; no leverage (position 0/1); a trade still open at the last bar closes there and the desk pays the liquidation fee in the equity path too.",
   "description": "clause S4"
  },
  {
   "id": "rule.S5.law",
   "kind": "value",
   "value": "Report total_return, CAGR, annualized Sharpe, max_drawdown, n_trades, win_rate, profit_factor, exposure — or refuse to print nonsense.",
   "description": "clause S5"
  },
  {
   "id": "rule.S6.law",
   "kind": "value",
   "value": "First wf_pct of bars = FIT (IS), rest = JUDGE (OOS). ROBUST: OOS sharpe >= 0.75, dd <= 40%, trades >= 3. Wins IS + loses OOS = OVERFIT. Always report both windows.",
   "description": "clause S6"
  },
  {
   "id": "rule.S7.law",
   "kind": "value",
   "value": "Promote a candidate only if its OOS score beats the champion AND its IS score does not degrade; score = sharpe - 2*maxdd - (0.5 if <3 trades). Every decision books a receipt.",
   "description": "clause S7"
  },
  {
   "id": "rule.S8.law",
   "kind": "value",
   "value": "Everything derives from closes + params + book; ai.ledger is an fnv1a64 chain rooted at GENESIS; S8 re-derives it; tampering breaks the chain visibly.",
   "description": "clause S8"
  },
  {
   "id": "rule.S1.check",
   "kind": "program",
   "code": "\nconst c = input?.closes ?? [];\nconst okLen = Array.isArray(c) && c.length >= 50;\nconst okVals = okLen && c.every(x => Number.isFinite(x) && x > 0);\nconst fired = !(okLen && okVals);\nreturn { fired, why: !fired\n  ? 'universe sealed: ' + c.length + ' finite positive daily closes (S1).'\n  : 'the series cannot be traded — len=' + c.length + ' (needs >= 50), all finite positive=' + okVals + ' (S1 stops the desk).' };",
   "description": "S1 port: universe validity (pure)",
   "deps": []
  },
  {
   "id": "rule.S2.check",
   "kind": "program",
   "code": "\nconst { strategy } = input ?? {};\nconst p = input?.params ?? {};\nconst ok = (strategy === 'sma_cross' || strategy === 'rsi_reversion')\n  && Number.isInteger(p.fast) && Number.isInteger(p.slow) && Number.isInteger(p.rsi_len)\n  && p.fast >= 2 && p.slow >= p.fast + 2 && p.rsi_len >= 2 && p.rsi_len <= 60\n  && p.rsi_max >= 50 && p.rsi_max <= 95\n  && p.rsi_buy >= 10 && p.rsi_buy <= 45 && p.rsi_sell >= 50 && p.rsi_sell <= 90 && p.rsi_buy < p.rsi_sell\n  && p.fee_bps >= 0 && p.fee_bps <= 50;\nreturn { fired: !ok, why: ok\n  ? 'params sane: ' + strategy + ' fast=' + p.fast + ' slow=' + p.slow + ' rsi_len=' + p.rsi_len + ' fee=' + p.fee_bps + 'bps (S2).'\n  : 'parameter set is not tradeable (S2: fast>=2, slow>=fast+2, 2<=rsi_len<=60, gates in range, fee 0..50bps).' };",
   "description": "S2 port: parameter sanity (pure)",
   "deps": []
  },
  {
   "id": "rule.S3.check",
   "kind": "program",
   "code": "\nconst { pos, closes, fast, slow, rsi_len } = input ?? {};\nconst same = Array.isArray(pos) && Array.isArray(closes) && pos.length === closes.length;\nconst binary = same && pos.every(x => x === 0 || x === 1);\nconst warm = Math.max(fast - 1, slow - 1, rsi_len);\nconst noEarly = same && pos.slice(0, Math.min(warm, pos.length)).every(x => x === 0);\nconst fired = !(same && binary && noEarly);\nreturn { fired, why: !fired\n  ? 'signal clean: ' + pos.length + ' bars, binary, flat through warmup bar ' + warm + ' (S3: no lookahead, no guesses).'\n  : 'signal malformed — same length=' + same + ', binary=' + binary + ', flat through warmup=' + noEarly + ' (S3 refuses).' };",
   "description": "S3 port: signal structure audit (pure)",
   "deps": []
  },
  {
   "id": "rule.S4.check",
   "kind": "program",
   "code": "\nconst { equity, fee_bps, cash0 } = input ?? {};\nconst finite = Array.isArray(equity) && equity.every(x => Number.isFinite(x));\nconst rooted = Array.isArray(equity) && Math.abs(equity[0] - cash0) < 1e-9;\nconst solvent = finite && equity.every(x => x >= -1e-6);\nconst fired = !(finite && rooted && solvent);\nreturn { fired, why: !fired\n  ? 'costs honored: equity starts at cash0, stays finite and solvent; every position change was priced at ' + fee_bps + 'bps (S4).'\n  : 'equity path invalid — finite=' + finite + ', rooted at cash0=' + rooted + ', solvent=' + solvent + ' (S4 refuses).' };",
   "description": "S4 port: equity path + costs (pure)",
   "deps": []
  },
  {
   "id": "rule.S5.check",
   "kind": "program",
   "code": "\nconst m = input?.metrics ?? {};\nconst keys = ['total_return', 'cagr', 'sharpe', 'maxdd', 'n_trades', 'win_rate', 'profit_factor', 'exposure'];\nconst finite = keys.every(k => Number.isFinite(m[k]));\nconst shape = Number.isInteger(m.n_trades) && m.n_trades >= 0\n  && m.maxdd >= 0 && m.maxdd <= 1 && m.exposure >= 0 && m.exposure <= 1\n  && m.win_rate >= 0 && m.win_rate <= 1 && m.total_return > -1;\nconst fired = !(finite && shape);\nreturn { fired, why: !fired\n  ? 'metrics sane: ret ' + (100 * m.total_return).toFixed(1) + '%, sharpe ' + m.sharpe.toFixed(2) + ', dd ' + (100 * m.maxdd).toFixed(1) + '%, ' + m.n_trades + ' trades (S5).'\n  : 'metrics are not printable — finite=' + finite + ', shapes ok=' + shape + ' (S5 stops the desk rather than show nonsense).' };",
   "description": "S5 port: metric sanity (pure)",
   "deps": []
  },
  {
   "id": "rule.S6.check",
   "kind": "program",
   "code": "\nconst { is_m, oos_m } = input ?? {};\nconst robust = oos_m.sharpe >= 0.75 && oos_m.maxdd <= 0.40 && oos_m.n_trades >= 3;\nconst overfit = is_m.sharpe > 0 && oos_m.sharpe <= 0;\nconst verdict = overfit ? 'OVERFIT' : (robust ? 'ROBUST' : 'WEAK');\nreturn { verdict, fired: verdict !== 'ROBUST', why: verdict === 'ROBUST'\n  ? 'ROBUST out of sample: sharpe ' + oos_m.sharpe.toFixed(2) + ' >= 0.75, dd ' + (100 * oos_m.maxdd).toFixed(1) + '% <= 40%, ' + oos_m.n_trades + ' trades (S6).'\n  : verdict === 'OVERFIT'\n  ? 'OVERFIT: in-sample sharpe ' + is_m.sharpe.toFixed(2) + ' but out-of-sample ' + oos_m.sharpe.toFixed(2) + ' — the fit memorized the fit window (S6).'\n  : 'WEAK out of sample: sharpe ' + oos_m.sharpe.toFixed(2) + ' does not clear the ROBUST bar (S6: 0.75 sharpe, 40% dd, 3+ trades).' };",
   "description": "S6 port: walk-forward verdict (pure)",
   "deps": []
  },
  {
   "id": "rule.S7.check",
   "kind": "program",
   "code": "\nconst { cand, champ } = input ?? {};\nconst beatOOS = cand.oos_score > champ.oos_score + 1e-9;\nconst noISDrop = cand.is_score >= champ.is_score - 1e-9;\nconst promote = beatOOS && noISDrop;\nreturn { promote, fired: !promote, why: promote\n  ? 'PROMOTED: OOS score ' + cand.oos_score.toFixed(3) + ' beats champion ' + champ.oos_score.toFixed(3) + ', IS score holds (S7).'\n  : 'REFUSED: ' + (!beatOOS\n      ? 'OOS score ' + cand.oos_score.toFixed(3) + ' does not beat champion ' + champ.oos_score.toFixed(3)\n      : 'IS score ' + cand.is_score.toFixed(3) + ' degrades champion ' + champ.is_score.toFixed(3)) + ' (S7 gate).' };",
   "description": "S7 port: promotion gate (pure)",
   "deps": []
  },
  {
   "id": "rule.S8.check",
   "kind": "program",
   "code": "\n\n    const fnv1a64 = (s) => { let h = 0xcbf29ce484222325n;\n      const p = 0x100000001b3n, m = 0xffffffffffffffffn;\n      for (let i = 0; i < s.length; i++) { h ^= BigInt(s.charCodeAt(i)); h = (h * p) & m; }\n      return h.toString(16).padStart(16, '0'); };\n    const canon = (row) => { const s = {}; for (const k of Object.keys(row).sort()) s[k] = row[k];\n      return JSON.stringify(s); };\n    const GENESIS_PREV = '0'.repeat(16);\nconst rows = input?.rows ?? [];\nconst fieldsOf = ({ row_hash, ts, ...fields }) => fields;\nlet prev = '0'.repeat(16);\ntry {\n  for (let i = 0; i < rows.length; i++) {\n    const expect = fnv1a64(canon({ ...fieldsOf(rows[i]), prev_hash: prev }));\n    if (rows[i].row_hash !== expect) throw new Error('hash mismatch at seq ' + (rows[i].seq ?? i));\n    if (rows[i].prev_hash !== prev) throw new Error('prev_hash mismatch at seq ' + (rows[i].seq ?? i));\n    prev = rows[i].row_hash;\n  }\n  return { ok: true, len: rows.length, head: prev, fired: false,\n    why: 'chain sealed: ' + rows.length + ' receipts re-derive from GENESIS, head ' + prev.slice(0, 8) + '… (S8).' };\n} catch (e) {\n  return { ok: false, len: rows.length, head: null, fired: true, why: 'TAMPERED: ' + e.message + ' (S8) — the ledger no longer proves its own history.' };\n}",
   "description": "S8 port: receipt chain re-derivation (pure)",
   "deps": []
  },
  {
   "id": "rule.S1.verdict",
   "kind": "value",
   "value": null,
   "description": "last S1 evaluation"
  },
  {
   "id": "rule.S2.verdict",
   "kind": "value",
   "value": null,
   "description": "last S2 evaluation"
  },
  {
   "id": "rule.S3.verdict",
   "kind": "value",
   "value": null,
   "description": "last S3 evaluation"
  },
  {
   "id": "rule.S4.verdict",
   "kind": "value",
   "value": null,
   "description": "last S4 evaluation"
  },
  {
   "id": "rule.S5.verdict",
   "kind": "value",
   "value": null,
   "description": "last S5 evaluation"
  },
  {
   "id": "rule.S6.verdict",
   "kind": "value",
   "value": null,
   "description": "last S6 evaluation"
  },
  {
   "id": "rule.S7.verdict",
   "kind": "value",
   "value": null,
   "description": "last S7 evaluation"
  },
  {
   "id": "rule.S8.verdict",
   "kind": "value",
   "value": null,
   "description": "last S8 evaluation"
  },
  {
   "id": "ind.sma_fast",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\nconst closes = (await runtime.get('mkt.closes')).data;\nconst p = (await runtime.get('p.fast')).data;\nreturn Q.sma(closes, p);",
   "description": "sma_fast line (S2)",
   "deps": [
    "mkt.closes",
    "p.fast"
   ]
  },
  {
   "id": "ind.sma_slow",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\nconst closes = (await runtime.get('mkt.closes')).data;\nconst p = (await runtime.get('p.slow')).data;\nreturn Q.sma(closes, p);",
   "description": "sma_slow line (S2)",
   "deps": [
    "mkt.closes",
    "p.slow"
   ]
  },
  {
   "id": "ind.rsi",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\nconst closes = (await runtime.get('mkt.closes')).data;\nconst n = (await runtime.get('p.rsi_len')).data;\nreturn Q.rsi(closes, n);",
   "description": "Wilder RSI line (S2)",
   "deps": [
    "mkt.closes",
    "p.rsi_len"
   ]
  },
  {
   "id": "sig.pos",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\nconst g = (id) => runtime.get(id).then(r => r.data);\nconst closes = await g('mkt.closes');\nconst strategy = await g('p.strategy');\nconst fast = await g('p.fast'), slow = await g('p.slow');\nconst rsi_len = await g('p.rsi_len'), rsi_max = await g('p.rsi_max');\nconst rsi_buy = await g('p.rsi_buy'), rsi_sell = await g('p.rsi_sell');\nreturn Q.pos(closes, strategy, fast, slow, rsi_len, rsi_max, rsi_buy, rsi_sell);",
   "description": "position series 0/1 — the strategy as a cell (S3)",
   "deps": [
    "mkt.closes",
    "p.strategy",
    "p.fast",
    "p.slow",
    "p.rsi_len",
    "p.rsi_max",
    "p.rsi_buy",
    "p.rsi_sell"
   ]
  },
  {
   "id": "bt.run",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\n// the desk's pricing engine — sequences the rule cells, applies S4/S5 itself.\nconst g = (id) => runtime.get(id).then(r => r.data);\nconst s = (id, val) => runtime.set(id, val);\nconst ts = Date.now();\nconst fired = [];\nconst closes = await g('mkt.closes');\nconst cash0 = await g('desk.cash0');\nconst s1 = (await runtime.call('rule.S1.check', { closes })).data;\nawait s('rule.S1.verdict', { ...s1, ts }); fired.push('S1');\nif (s1.fired) { const r = { ok: false, rule: 'S1', text: s1.why, fired }; await s('bt.last', r); return r; }\n\nconst strategy = await g('p.strategy');\nconst params = { fast: await g('p.fast'), slow: await g('p.slow'), rsi_len: await g('p.rsi_len'),\n  rsi_max: await g('p.rsi_max'), rsi_buy: await g('p.rsi_buy'), rsi_sell: await g('p.rsi_sell'),\n  fee_bps: await g('p.fee_bps') };\nconst s2 = (await runtime.call('rule.S2.check', { strategy, params })).data;\nawait s('rule.S2.verdict', { ...s2, ts }); fired.push('S2');\nif (s2.fired) { const r = { ok: false, rule: 'S2', text: s2.why, fired, strategy, params }; await s('bt.last', r); return r; }\n\nconst pos = (await runtime.get('sig.pos')).data;\nconst s3 = (await runtime.call('rule.S3.check', { pos, closes, fast: params.fast, slow: params.slow, rsi_len: params.rsi_len })).data;\nawait s('rule.S3.verdict', { ...s3, ts }); fired.push('S3');\nif (s3.fired) { const r = { ok: false, rule: 'S3', text: s3.why, fired, strategy, params }; await s('bt.last', r); return r; }\n\nconst bt = Q.backtest(closes, pos, params.fee_bps, cash0);\nconst s4 = (await runtime.call('rule.S4.check', { equity: bt.equity, fee_bps: params.fee_bps, cash0 })).data;\nawait s('rule.S4.verdict', { ...s4, ts }); fired.push('S4');\nif (s4.fired) { const r = { ok: false, rule: 'S4', text: s4.why, fired, strategy, params }; await s('bt.last', r); return r; }\n\nconst s5 = (await runtime.call('rule.S5.check', { metrics: bt.metrics })).data;\nawait s('rule.S5.verdict', { ...s5, ts }); fired.push('S5');\nif (s5.fired) { const r = { ok: false, rule: 'S5', text: s5.why, fired, strategy, params }; await s('bt.last', r); return r; }\n\n// PUBLISH the receipt as a value cell — every derived formula reads the\n// published receipt, so the desk is always a consistent snapshot.\nconst receipt = { ok: true, fired, strategy, params, equity: bt.equity, metrics: bt.metrics, trades: bt.trades, ts };\nawait s('bt.last', receipt);\nreturn receipt;",
   "description": "the desk: S1→S2→S3→backtest(S4,S5) — full receipt",
   "deps": [
    "mkt.closes",
    "desk.cash0",
    "p.strategy",
    "p.fast",
    "p.slow",
    "p.rsi_len",
    "p.rsi_max",
    "p.rsi_buy",
    "p.rsi_sell",
    "p.fee_bps",
    "sig.pos",
    "rule.S1.check",
    "rule.S2.check",
    "rule.S3.check",
    "rule.S4.check",
    "rule.S5.check"
   ]
  },
  {
   "id": "bnh.run",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\n// buy & hold — the control group every strategy must beat or explain\nconst closes = (await runtime.get('mkt.closes')).data;\nconst cash0 = (await runtime.get('desk.cash0')).data;\nconst ones = new Array(closes.length).fill(1);\nconst bt = Q.backtest(closes, ones, 0, cash0);\nconst receipt = { ok: true, metrics: bt.metrics, equity: bt.equity };\nawait runtime.set('bnh.last', receipt);\nreturn receipt;",
   "description": "buy & hold — the control group",
   "deps": [
    "mkt.closes",
    "desk.cash0"
   ]
  },
  {
   "id": "wf.report",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\n// walk-forward: refit-window vs judge-window for the CURRENT desk params.\nconst g = (id) => runtime.get(id).then(r => r.data);\nconst s = (id, val) => runtime.set(id, val);\nconst ts = Date.now();\nconst closes = await g('mkt.closes');\nconst cash0 = await g('desk.cash0');\nconst strategy = await g('p.strategy');\nconst params = { fast: await g('p.fast'), slow: await g('p.slow'), rsi_len: await g('p.rsi_len'),\n  rsi_max: await g('p.rsi_max'), rsi_buy: await g('p.rsi_buy'), rsi_sell: await g('p.rsi_sell'),\n  fee_bps: await g('p.fee_bps') };\nconst pct = await g('p.wf_pct');\nconst { isN, oosN } = Q.wfSplit(closes.length, pct);\nconst runSlice = (c) => {\n  const pos = Q.pos(c, strategy, params.fast, params.slow, params.rsi_len, params.rsi_max, params.rsi_buy, params.rsi_sell);\n  return Q.backtest(c, pos, params.fee_bps, cash0).metrics;\n};\nconst is_m = runSlice(closes.slice(0, isN));\nconst oos_m = runSlice(closes.slice(isN));\nconst s6 = (await runtime.call('rule.S6.check', { is_m, oos_m })).data;\nawait s('rule.S6.verdict', { ...s6, ts });\nconst report = { ok: true, isN, oosN, strategy, params, is_m, oos_m, verdict: s6.verdict, why: s6.why };\nawait s('wf.last', report);\nreturn report;",
   "description": "walk-forward: IS vs OOS for current params (S6)",
   "deps": [
    "mkt.closes",
    "desk.cash0",
    "p.strategy",
    "p.fast",
    "p.slow",
    "p.rsi_len",
    "p.rsi_max",
    "p.rsi_buy",
    "p.rsi_sell",
    "p.fee_bps",
    "p.wf_pct",
    "rule.S6.check"
   ]
  },
  {
   "id": "met.ret",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.total_return : null",
   "description": "total return (S5)"
  },
  {
   "id": "met.cagr",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.cagr : null",
   "description": "CAGR, 252d year (S5)"
  },
  {
   "id": "met.sharpe",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.sharpe : null",
   "description": "annualized Sharpe (S5)"
  },
  {
   "id": "met.maxdd",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.maxdd : null",
   "description": "max drawdown (S5)"
  },
  {
   "id": "met.trades",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.n_trades : null",
   "description": "number of round trips (S5)"
  },
  {
   "id": "met.winrate",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.win_rate : null",
   "description": "per-trade win rate incl. fees (S5)"
  },
  {
   "id": "met.pf",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.profit_factor : null",
   "description": "profit factor (S5)"
  },
  {
   "id": "met.exposure",
   "kind": "formula",
   "expr": "bt.last && bt.last.metrics ? bt.last.metrics.exposure : null",
   "description": "time in the market (S5)"
  },
  {
   "id": "met.wf",
   "kind": "formula",
   "expr": "wf.last ? wf.last.verdict : null",
   "description": "walk-forward verdict (S6)"
  },
  {
   "id": "met.bnh_ret",
   "kind": "formula",
   "expr": "bnh.last && bnh.last.metrics ? bnh.last.metrics.total_return : null",
   "description": "buy & hold total return"
  },
  {
   "id": "ai.ledger",
   "kind": "value",
   "value": [],
   "description": "witness receipts: seed / fit / refuse / promote (fnv1a64 chain, S8)"
  },
  {
   "id": "ai.trainer",
   "kind": "program",
   "code": "\n\n// Q — the pure quant kernel, inlined into every pricing cell (new-Function\n// scope: helpers must live inside the cell — the E8 lesson).\nconst Q = {\n  sma(c, w) { const n = c.length, out = new Array(n).fill(null); let s = 0;\n    for (let i = 0; i < n; i++) { s += c[i]; if (i >= w) s -= c[i - w]; if (i >= w - 1) out[i] = s / w; }\n    return out; },\n  rsi(c, n) { const L = c.length, out = new Array(L).fill(null);\n    if (L < n + 1) return out;\n    let g = 0, l = 0; for (let i = 1; i <= n; i++) { const d = c[i] - c[i - 1]; if (d >= 0) g += d; else l -= d; }\n    let ag = g / n, al = l / n; out[n] = al === 0 ? 100 : 100 - 100 / (1 + ag / al);\n    for (let i = n + 1; i < L; i++) { const d = c[i] - c[i - 1], gg = d > 0 ? d : 0, ll = d < 0 ? -d : 0;\n      ag = (ag * (n - 1) + gg) / n; al = (al * (n - 1) + ll) / n; out[i] = al === 0 ? 100 : 100 - 100 / (1 + ag / al); }\n    return out; },\n  pos(c, strategy, fast, slow, rsiLen, rsiMax, rsiBuy, rsiSell) {\n    const f = Q.sma(c, fast), s = Q.sma(c, slow), r = Q.rsi(c, rsiLen);\n    const p = new Array(c.length).fill(0);\n    for (let i = 0; i < c.length; i++) {\n      if (f[i] == null || s[i] == null || r[i] == null) { p[i] = 0; continue; }\n      if (strategy === 'rsi_reversion') {\n        if (r[i] <= rsiBuy) p[i] = 1;\n        else if (r[i] >= rsiSell) p[i] = 0;\n        else p[i] = (i > 0 ? p[i - 1] : 0);\n      } else {\n        p[i] = (f[i] > s[i] && r[i] <= rsiMax) ? 1 : 0;\n      }\n    }\n    return p; },\n  backtest(c, p, feeBps, cash0) {\n    const n = c.length; const eq = new Array(n); eq[0] = cash0;\n    const fr = feeBps / 10000; const trades = [];\n    let cur = null, tPnl = 0;\n    for (let i = 1; i < n; i++) {\n      const pnl = p[i - 1] * (c[i] - c[i - 1]);\n      const fee = (p[i] !== p[i - 1]) ? fr * c[i] * Math.abs(p[i] - p[i - 1]) : 0;\n      eq[i] = eq[i - 1] + pnl - fee;\n      if (p[i] === 1 && p[i - 1] !== 1) { cur = { entry: i }; tPnl = -fee; }\n      else if (cur && p[i] !== 1) { tPnl += pnl - fee; cur.exit = i; cur.pnl = tPnl; trades.push(cur); cur = null; }\n      else if (cur) { tPnl += pnl; }\n    }\n    // S4: a trade still open at the last bar closes there — the desk pays the\n    // liquidation fee in the EQUITY path too, so trades and equity always agree\n    if (cur) { eq[n - 1] -= fr * c[n - 1]; cur.exit = n - 1; cur.pnl = tPnl - fr * c[n - 1]; trades.push(cur); }\n    let wins = 0, grossWin = 0, grossLoss = 0;\n    for (const t of trades) { if (t.pnl >= 0) { wins++; grossWin += t.pnl; } else grossLoss += -t.pnl; }\n    let peak = eq[0], maxdd = 0, expo = 0;\n    for (let i = 1; i < n; i++) {\n      if (eq[i] < peak) { const dd = (peak - eq[i]) / peak; if (dd > maxdd) maxdd = dd; } else peak = eq[i];\n      if (p[i - 1] > 0) expo++;\n    }\n    let sum = 0, sum2 = 0, cnt = 0;\n    for (let i = 1; i < n; i++) { const r = eq[i] / eq[i - 1] - 1; sum += r; sum2 += r * r; cnt++; }\n    const mean = sum / cnt, sd = Math.sqrt(Math.max(sum2 / cnt - mean * mean, 0));\n    const years = (n - 1) / 252, last = eq[n - 1];\n    const metrics = {\n      total_return: last / cash0 - 1,\n      cagr: last > 0 ? Math.pow(last / cash0, 1 / years) - 1 : -1,\n      sharpe: sd > 0 ? mean / sd * Math.sqrt(252) : 0,\n      maxdd, n_trades: trades.length,\n      win_rate: trades.length ? wins / trades.length : 0,\n      profit_factor: grossLoss > 0 ? grossWin / grossLoss : (grossWin > 0 ? 99 : 0),\n      exposure: expo / (n - 1),\n    };\n    return { equity: eq, metrics, trades };\n  },\n  score(m) { return m.sharpe - 2 * m.maxdd - (m.n_trades < 3 ? 0.5 : 0); },\n  wfSplit(n, pct) { const isN = Math.max(30, Math.min(n - 30, Math.floor(n * pct / 100))); return { isN, oosN: n - isN }; },\n  paramsOk(strategy, p) {\n    if (!Number.isInteger(p.fast) || !Number.isInteger(p.slow) || !Number.isInteger(p.rsi_len)) return false;\n    if (p.fast < 2 || p.slow < p.fast + 2 || p.rsi_len < 2 || p.rsi_len > 60) return false;\n    if (p.rsi_max < 50 || p.rsi_max > 95) return false;\n    if (p.rsi_buy < 10 || p.rsi_buy > 45 || p.rsi_sell < 50 || p.rsi_sell > 90 || p.rsi_buy >= p.rsi_sell) return false;\n    if (p.fee_bps < 0 || p.fee_bps > 50) return false;\n    return strategy === 'sma_cross' || strategy === 'rsi_reversion';\n  },\n  perturb(strategy, p, rnd) {\n    const clamp = (x, a, b) => Math.max(a, Math.min(b, x));\n    const st = rnd() < 0.10 ? (strategy === 'sma_cross' ? 'rsi_reversion' : 'sma_cross') : strategy;\n    const q = { ...p };\n    const knob = ['fast', 'slow', 'rsi_len', 'rsi_max', 'rsi_buy', 'rsi_sell'][Math.floor(rnd() * 6)];\n    const step = { fast: 1 + Math.floor(rnd() * 5), slow: 2 + Math.floor(rnd() * 8), rsi_len: 1 + Math.floor(rnd() * 3),\n      rsi_max: 2 + Math.floor(rnd() * 6), rsi_buy: 2 + Math.floor(rnd() * 6), rsi_sell: 2 + Math.floor(rnd() * 6) }[knob];\n    q[knob] = clamp((rnd() < 0.5 ? q[knob] - step : q[knob] + step),\n      { fast: 2, slow: 4, rsi_len: 2, rsi_max: 50, rsi_buy: 10, rsi_sell: 50 }[knob],\n      { fast: 60, slow: 160, rsi_len: 40, rsi_max: 95, rsi_buy: 45, rsi_sell: 90 }[knob]);\n    if (q.slow < q.fast + 2) q.slow = q.fast + 2;\n    if (q.rsi_buy >= q.rsi_sell) q.rsi_sell = q.rsi_buy + 5;\n    return { strategy: st, params: q };\n  },\n};\n\n    const rng = (seed) => { let a = (seed ?? 1) >>> 0;\n      return () => { a |= 0; a = (a + 0x6D2B79F5) | 0;\n        let t = Math.imul(a ^ (a >>> 15), 1 | a);\n        t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;\n        return ((t ^ (t >>> 14)) >>> 0) / 4294967296; }; };\n\n    const fnv1a64 = (s) => { let h = 0xcbf29ce484222325n;\n      const p = 0x100000001b3n, m = 0xffffffffffffffffn;\n      for (let i = 0; i < s.length; i++) { h ^= BigInt(s.charCodeAt(i)); h = (h * p) & m; }\n      return h.toString(16).padStart(16, '0'); };\n    const canon = (row) => { const s = {}; for (const k of Object.keys(row).sort()) s[k] = row[k];\n      return JSON.stringify(s); };\n    const GENESIS_PREV = '0'.repeat(16);\n// THE TRAINER — self-improvement through the gated pipeline.\n// selection on IS (the workbench), promotion only through the S7 OOS gate.\nconst g = (id) => runtime.get(id).then(r => r.data);\nconst s = (id, val) => runtime.set(id, val);\nconst logEv = async (e) => { const lg = await g('log.events'); await s('log.events', [...lg.slice(-199), { ts: Date.now(), ...e }]); };\n\nconst closes = await g('mkt.closes');\nconst cash0 = await g('desk.cash0');\nconst feeBps = await g('p.fee_bps');\nconst { isN } = Q.wfSplit(closes.length, await g('p.wf_pct'));\nconst IS = closes.slice(0, isN), OOS = closes.slice(isN);\n\nconst ledger = [...((await g('ai.ledger')) ?? [])];\nlet prev = ledger.length ? ledger[ledger.length - 1].row_hash : GENESIS_PREV;\nconst book = async (fields) => {\n  const seq = ledger.length + 1;\n  const row = { ...fields, seq, prev_hash: prev };\n  row.row_hash = fnv1a64(canon(row));\n  ledger.push({ ...row, ts: Date.now() });\n  prev = row.row_hash;\n  await s('ai.ledger', [...ledger]);\n};\n\nconst evalCand = (strategy, prm) => {\n  const run = (c) => {\n    const pos = Q.pos(c, strategy, prm.fast, prm.slow, prm.rsi_len, prm.rsi_max, prm.rsi_buy, prm.rsi_sell);\n    return Q.backtest(c, pos, feeBps, cash0).metrics;\n  };\n  const is_m = run(IS), oos_m = run(OOS);\n  return { is_m, oos_m, is_score: Q.score(is_m), oos_score: Q.score(oos_m) };\n};\n\n// seed the champion from the desk's current params (a real bar to beat)\nconst curStrategy = await g('p.strategy');\nlet champParams = { fast: await g('p.fast'), slow: await g('p.slow'), rsi_len: await g('p.rsi_len'),\n  rsi_max: await g('p.rsi_max'), rsi_buy: await g('p.rsi_buy'), rsi_sell: await g('p.rsi_sell') };\nlet champStrategy = curStrategy;\nconst seedEv = evalCand(champStrategy, champParams);\nlet champ = { strategy: champStrategy, params: { ...champParams }, is_score: seedEv.is_score, oos_score: seedEv.oos_score };\nawait book({ kind: 'seed', gen: 0, strategy: champStrategy, params: { ...champParams },\n  is_score: seedEv.is_score, oos_score: seedEv.oos_score, verdict: 'SEED', kept: true,\n  why: 'champion seeded from the desk: OOS score ' + seedEv.oos_score.toFixed(3) + '.' });\n\nconst rnd = rng(input?.seed ?? 7);\nconst gens = Math.max(1, Math.min(200, input?.gens ?? 24));\nlet workbench = { strategy: champ.strategy, params: { ...champ.params }, is_score: champ.is_score };\nlet tried = 0, kept = 0, lastVerdict = 'SEED';\n\nfor (let gen = 1; gen <= gens; gen++) {\n  let cand = Q.perturb(workbench.strategy, workbench.params, rnd);\n  if (gen === 1 && input?.force_cand) cand = input.force_cand; // the LLM strategist's lever\n  if (!Q.paramsOk(cand.strategy, cand.params)) {\n    await book({ kind: 'refuse', gen, strategy: cand.strategy, params: cand.params,\n      is_score: null, oos_score: null, verdict: 'S2', kept: false, why: 'candidate params not tradeable (S2).' });\n    continue;\n  }\n  tried++;\n  const ev = evalCand(cand.strategy, cand.params);\n  const s6 = (await runtime.call('rule.S6.check', { is_m: ev.is_m, oos_m: ev.oos_m })).data;\n  lastVerdict = s6.verdict;\n  let wbMoved = false;\n  if (ev.is_score > workbench.is_score) { workbench = { strategy: cand.strategy, params: { ...cand.params }, is_score: ev.is_score }; wbMoved = true; }\n  const s7 = (await runtime.call('rule.S7.check',\n    { cand: { is_score: ev.is_score, oos_score: ev.oos_score }, champ: { is_score: champ.is_score, oos_score: champ.oos_score } })).data;\n  await s('rule.S7.verdict', { ...s7, ts: Date.now(), gen });\n  if (s7.promote) {\n    champ = { strategy: cand.strategy, params: { ...cand.params }, is_score: ev.is_score, oos_score: ev.oos_score };\n    champParams = { ...cand.params }; champStrategy = cand.strategy;\n    workbench = { strategy: cand.strategy, params: { ...cand.params }, is_score: ev.is_score };\n    kept++;\n    await book({ kind: 'promote', gen, strategy: cand.strategy, params: { ...cand.params },\n      is_score: ev.is_score, oos_score: ev.oos_score, verdict: s6.verdict, kept: true, wb_moved: true, why: s7.why });\n  } else {\n    // the gate speaks: every non-promotion is a REFUSE receipt (the workbench\n    // may still have adopted the fit for IS — that is wb_moved, not kept)\n    await book({ kind: 'refuse', gen, strategy: cand.strategy, params: { ...cand.params },\n      is_score: ev.is_score, oos_score: ev.oos_score, verdict: s6.verdict, kept: false, wb_moved: wbMoved, why: s7.why });\n  }\n}\n\n// THE PROMOTION CASCADE — write the champion and its params; the reactive\n// graph re-prices indicators, signal, backtest, metrics, walk-forward, art.\nawait s('desk.champion', { gen: gens, strategy: champ.strategy, params: { ...champ.params },\n  is_score: champ.is_score, oos_score: champ.oos_score, verdict: lastVerdict });\nawait s('p.strategy', champ.strategy);\nawait s('p.fast', champ.params.fast); await s('p.slow', champ.params.slow);\nawait s('p.rsi_len', champ.params.rsi_len); await s('p.rsi_max', champ.params.rsi_max);\nawait s('p.rsi_buy', champ.params.rsi_buy); await s('p.rsi_sell', champ.params.rsi_sell);\nawait logEv({ kind: 'trainer', text: 'trainer: ' + tried + ' candidates, ' + kept + ' promotions — champion gen ' + gens + ' OOS score ' + champ.oos_score.toFixed(3) });\nreturn { ok: true, gens, tried, kept, champion: { ...champ }, ledger_len: ledger.length, head: prev };",
   "description": "the self-improvement loop: IS selection, OOS promotion gate, receipts",
   "deps": [
    "mkt.closes",
    "desk.cash0",
    "p.strategy",
    "p.fast",
    "p.slow",
    "p.rsi_len",
    "p.rsi_max",
    "p.rsi_buy",
    "p.rsi_sell",
    "p.fee_bps",
    "p.wf_pct",
    "ai.ledger",
    "desk.champion",
    "log.events",
    "rule.S6.check",
    "rule.S7.check"
   ]
  },
  {
   "id": "ai.chaincheck",
   "kind": "program",
   "code": "\n\n    const fnv1a64 = (s) => { let h = 0xcbf29ce484222325n;\n      const p = 0x100000001b3n, m = 0xffffffffffffffffn;\n      for (let i = 0; i < s.length; i++) { h ^= BigInt(s.charCodeAt(i)); h = (h * p) & m; }\n      return h.toString(16).padStart(16, '0'); };\n    const canon = (row) => { const s = {}; for (const k of Object.keys(row).sort()) s[k] = row[k];\n      return JSON.stringify(s); };\n    const GENESIS_PREV = '0'.repeat(16);\nconst rows = (await runtime.get('ai.ledger')).data ?? [];\nconst v8 = (await runtime.call('rule.S8.check', { rows })).data;\nawait runtime.set('rule.S8.verdict', { ...v8, ts: Date.now() });\nreturn v8;",
   "description": "S8 in the loop: re-derive the receipt chain",
   "deps": [
    "ai.ledger",
    "rule.S8.check"
   ]
  },
  {
   "id": "art.equity",
   "kind": "program",
   "code": "\n// the desk's glass: equity + drawdown as unicode sparklines\nconst g = (id) => runtime.get(id).then(r => r.data);\nconst spark = (arr) => {\n  let lo = Infinity, hi = -Infinity;\n  for (const x of arr) { if (x < lo) lo = x; if (x > hi) hi = x; }\n  const glyphs = '▁▂▃▄▅▆▇█'; const span = (hi - lo) || 1;\n  const step = Math.max(1, Math.floor(arr.length / 88));\n  let s2 = '';\n  for (let i = 0; i < arr.length; i += step) s2 += glyphs[Math.min(7, Math.floor((arr[i] - lo) / span * 7.999))];\n  return s2;\n};\nconst bt = await g('bt.last');\nconst bnh = await g('bnh.last');\nif (!bt?.ok || !bnh?.equity) return { ok: false, desk: '', bnh: '', dd: '', why: 'pull bt.run and bnh.run first — the glass shows the last completed run.' };\nconst eq = bt.equity;\nlet peak = eq[0]; const dd = eq.map(x => { peak = Math.max(peak, x); return x < peak ? (peak - x) / peak : 0; });\nreturn { ok: true, desk: spark(eq), bnh: spark(bnh.equity), dd: spark(dd.map(x => -x)) };",
   "description": "equity + drawdown sparklines for the glass",
   "deps": [
    "bt.last",
    "bnh.last"
   ]
  },
  {
   "id": "champion.announce",
   "kind": "program",
   "code": "\n// fires when desk.champion changes — the desk flashes the promotion\nconst g = (id) => runtime.get(id).then(r => r.data);\nconst s = (id, val) => runtime.set(id, val);\nconst champ = await g('desk.champion');\nconst was = await g('desk.prev_champ');\nif (!champ) return { skipped: true };\nconst same = was && was.gen === champ.gen && was.oos_score === champ.oos_score && was.params?.fast === champ.params?.fast;\nif (same) return { skipped: true };\nconst promoted = was && champ.oos_score != null && was.oos_score != null && champ.oos_score > was.oos_score;\nawait s('desk.flash', {\n  kind: promoted ? 'promote' : 'champion',\n  text: promoted\n    ? 'NEW CHAMPION — OOS score ' + was.oos_score.toFixed(3) + ' → ' + champ.oos_score.toFixed(3) + ' (' + champ.strategy + ', fast ' + champ.params.fast + '/slow ' + champ.params.slow + '). Desk re-priced.'\n    : 'champion at gen ' + champ.gen + ' — OOS score ' + (champ.oos_score == null ? '—' : champ.oos_score.toFixed(3)) + '.',\n  ts: Date.now() });\nawait s('desk.prev_champ', champ ? { ...champ, params: { ...champ.params } } : null);\nconst lg = await g('log.events');\nawait s('log.events', [...lg.slice(-199), { ts: Date.now(), kind: 'champion', text: (promoted ? 'PROMOTED: ' : 'champion: ') + (champ.oos_score?.toFixed?.(3) ?? '—') + ' (' + champ.strategy + ')' }]);\nreturn { ok: true, promoted };",
   "description": "writes the promotion flash + log",
   "deps": [
    "desk.champion",
    "desk.prev_champ",
    "log.events"
   ]
  },
  {
   "id": "champion.push",
   "kind": "listener",
   "watch": [
    "desk.champion"
   ],
   "action": "champion.announce",
   "description": "watches the champion seat — a promotion lights the desk up"
  }
 ]
}